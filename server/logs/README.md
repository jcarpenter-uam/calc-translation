Log directory for per session logs with a nested structure

For example:

```
./{integration}/{session}
    - {integration}_{session}.log
./{integration}/{session}
    - {integration}_{session}.log
```
