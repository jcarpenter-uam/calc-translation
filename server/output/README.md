Output directory for per session output with a nested structure, this is where downloadables will be stored

For example:

```
./{integration}/{session}
    - transcipt.vtt
./{integration}/{session}
    - transcipt.vtt
```
