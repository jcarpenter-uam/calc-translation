# File to handle logging uniformly
# Verbose log files should be saved per session at ./logs/{integration}/{session}/{integration}_{session}.log
# Console logs are determined by the LOG_LEVEL env variable with debug, info, error levels
